[project]
name = "paim"
version = "0.1.0"
description = "LLM 기반 AI 프로젝트 매니저 — 의사결정 맥락 기억 서비스"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "python-multipart>=0.0.20",
    "uvicorn",
    "anthropic",
    "openai",
    "google-generativeai",
    "pymysql",
    "chromadb",
    "cryptography",
    "langchain-openai",
    "langchain-anthropic",
    "langchain-google-genai",
    "langchain-chroma",
    "langgraph",
    "pydantic",
    "python-dotenv",
    "pypdf>=6.14.2",
    "python-docx>=1.1.2",
    "tiktoken",
    "rank-bm25>=0.2.2",
    "kiwipiepy>=0.23.2",
    "bcrypt>=5.0.0",
    "slowapi>=0.1.10",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["backend"]

[dependency-groups]
dev = [
    "httpx",
    "httpx2>=2.9.1",
    "pytest",
    "pytest-asyncio",
]
eval = [
    "ragas>=0.4.3",
    "openpyxl>=3.1",   # EVAL_REPORT.xlsx export(pandas ExcelWriter 엔진)
]

[project.scripts]
paim-server = "backend.main:serve"
