# 문서 적재 E2E 검증 보고서 — DOCX·PDF → Project Memory

> 대상: `backend/pipeline/converters/` 멀티포맷 입력 + 출처 추적 청킹 (PR #1)
> 환경: MySQL 8.0(docker) · ChromaDB(임베디드) · OpenAI `gpt-4.1-mini` · `PAIM_AUTH_MODE=dev`
> 전처리 규칙은 [문서 전처리 정책서](DOCUMENT_INGESTION_POLICY.md), 응답 계약은
> [API 명세서](API_명세서.md) 참고.

## 1. 검증 조건

`origin/main`(`#7` 운영 하드닝 · `#6` 포맷 동기화) 병합으로 업로드 경로가 재작성
수준으로 바뀌어, 병합 전후 두 번 실행했다. **결정론적 산출물은 동일했다.**

| 지표 | 병합 전 `d73bd04` | 병합 후 `5e124a4` |
| --- | ---: | ---: |
| 파일 `indexed` / `documents` | 9 / 9 | 9 / 9 |
| **문서 청크** | **74** | **74** |
| **출처 metadata 완비** | **74/74** | **74/74** |
| `memory` | 90 | 88 |

`memory` 차이는 LLM(`gpt-4.1-mini`) 추출 편차다. 변환·청킹 산출물이 같으므로 병합이
파이프라인에 영향을 주지 않았다고 판단한다(독립 리뷰가 `extractor.py` blob 동일까지
확인). **아래 결과는 병합 후 기준이다.**

DB·ChromaDB를 완전히 비운 상태에서 시작했다.

```bash
docker compose down -v      # MySQL 볼륨 삭제
rm -rf .chroma              # 벡터 제거
docker compose up -d        # 스키마 자동 재적용
# 기동 직후: 테이블 15개, projects/documents/memory 모두 0행 확인
```

### 결과 — 완료 조건 충족

```
파일 9건 전부 indexed 도달
projects 9 · documents 9 · memory 88 · memory_sources 88 · project_memory 9
ChromaDB 162 벡터 = 문서 청크 74 + 메모리 항목 88
출처 metadata 완비 74/74
source_format 분포: docx 40 / pdf 34
action 45건 전부 owner 보유
빈 파일 → 400 empty_document
```

| 파일 | format | blocks | 경고 | 상태 |
| --- | --- | ---: | ---: | --- |
| 01 정형 회의록 | docx | 63 | 5 | indexed |
| 01 정형 회의록 | pdf | 26 | 1 | indexed |
| 02 자유양식 | docx | 64 | 0 | indexed |
| 02 자유양식 | pdf | 34 | 1 | indexed |
| 03 정형 가명화 | docx | 63 | 5 | indexed |
| 03 정형 가명화 | pdf | 26 | 1 | indexed |
| 04 자유양식 가명화 | docx | 64 | 0 | indexed |
| 04 자유양식 가명화 | pdf | 35 | 1 | indexed |
| 05 중첩표 | docx | 67 | 7 | indexed |

### 병합 후 추가 확인

```
중첩 표 보존       05 샘플 3,393자 / 67 blocks / 경고 7 — 병합 전과 동일
파싱 경로 단일화    extract_document_text(...) == convert(...).text  → True
레지스트리 일치     document_content == converters = {.docx .markdown .md .pdf .txt}
제어문자 차단       U+007F DOCX → INVALID_DOCUMENT_CONTENT (정규화 전 검사)
```

### 재현

```bash
cp .env.example .env        # OPENAI_API_KEY, DB_PASSWORD 입력
                            # PAIM_AUTH_MODE=dev, DEV_USER_ID=1 추가
docker compose down -v && rm -rf .chroma && docker compose up -d
uv run uvicorn backend.main:app --host 127.0.0.1 --port 8000
# 프로젝트 생성 → POST /api/v1/projects/{id}/documents → status 폴링
```

> `PAIM_AUTH_MODE=dev`는 E2E 전용이다. `.env`에 남겨두면 CORS·auth 테스트가
> 그 값을 읽어 실패하므로, 검증 후 제거해야 한다.

---

## 2. 잘 된 것

**action 추출이 완전히 안정적이다.** 9개 조합 전부 정확히 5건, 담당자 5/5.
양식·포맷·가명화·중첩 어느 축으로도 흔들리지 않았다. 정답과 100% 일치한다.

**가명화가 추출에 영향을 주지 않는다.** 가명 인물명("참석자 A")도 담당자로 정확히
인식됐다.

**빈 파일이 정확히 거절된다.** `400` + `code: empty_document`,
`detail`은 `"문서에서 추출된 텍스트가 없습니다."` 19자.

**변환 경고가 실제 문서에서 의도대로 동작한다.** 정형 DOCX는 표 5개 → 경고 5건,
중첩표 문서는 최상위 표 7개 → 경고 7건(중첩 표는 별도 번호를 소비하지 않음),
PDF는 반복 머리말 → `repeated_line_dropped` 1건. 과다 생성 없음.

**`location`이 정확히 붙는다.** `table 1`, `table 2`, `page N` 형식으로
`R402`가 명세에 기록한 내용과 일치한다.

## 3. 표는 "안 되는" 것이 아니다 — 표별 대조 결과

**초판의 판단("표 안의 내용이 추출되지 않는다")은 틀렸다.** 숫자만 보고 어느 표에서
무엇이 나왔는지 대조하지 않은 결과다. 정형 문서(03)의 표 5개를 추출된 `memory`
본문과 문자열 대조했다.

| 표 | 내용 | 추출 | 판정 |
| --- | --- | :---: | --- |
| 표1 회의 정보 | 회의명·일시·장소·참석 인원 | ✗ | **정상** — 메타데이터. decision/action/issue/risk 어디에도 해당 없음 |
| 표2 참석자 | 성명·역할·담당 관점 | ✗ | **정상** — 명단. 카테고리 해당 없음 |
| 표3 주요 안건 | 안건 1~4 | ✗ | **정상** — 결론이 "잠정 결론" 절에서 decision으로 추출됨. 중복 회피 |
| 표4 대안별 검토 | 기대효과·**리스크**·판단·후속조치 | ✗ | **결함** — risk 0건 |
| 표5 담당자별 조사 과제 | 담당·산출물·**기한** | **✓ 5/5** | **정상** — owner 5/5, 기한이 content에 포함 |

```
[action] owner=참석자 A  참석자 A가 ... 블루프린트 초안 작성하겠습니다 (~7/28까지).
[action] owner=참석자 E  참석자 E가 ... 대체 도시 2곳 비교하겠습니다 (~7/28까지).
```

**5개 중 3개는 안 뽑히는 것이 맞다. 실제 결함은 표4 하나다.**

### 차이는 표 형식이 아니라 행의 완결성

**표5가 되는 이유**: 각 행이 그 자체로 완결된 액션이다. `누가 / 무엇을 / 언제까지`가
한 행에 모여 있어 `MemoryItem`(category·owner·content) 스키마에 그대로 대응된다.

**표4가 안 되는 이유**: 리스크가 열 값으로 흩어져 있다.

```
[표4] 전통 콘셉트 앨범 | 인지도·확산력 높음 | 섭외·제작비, 전통성 왜곡 위험 | 우선 검토 | ...
                                          ↑ 이 셀이 '리스크'라는 표시가 행에 없음
```

열 머리글은 첫 행에만 있고 데이터 행은 그 정보를 갖지 않는다.
LLM은 이 줄을 "대안 비교표의 한 줄"로 읽는다.

### 확실하지 않은 부분

- 표4형을 개선하는 방법 — **평탄화 형식 변경은 효과가 없다는 것만 확인됐다.**
  열 머리글 부착·마크다운 표로 바꿔도 결과가 동일했다(각 4/4).
- 정형 문서에서 issue가 0건인 이유 — 정형 문서에 issue에 해당하는 절이 없어
  **정답 자체가 불분명하다.**

## 4. 중첩 표 — 변환은 완전, 하위 행 추출은 누락

`05_PaiM_정형_회의록_Kculture_중첩표.docx`는 표 16개 중 9개가 중첩이며 **3단 중첩**을
포함한다(`1단 7개 / 2단 5개 / 3단 4개`).

### 변환: 유실 0

중첩 표에만 있는 고유 문자열이 전부 보존됐다.

| 검증 문자열 | 결과 |
| --- | --- |
| 국악 편곡가 섭외 | 보존 |
| 국악 연주자 세션 | 보존 |
| 쇼케이스 장소 후보 | 보존 |
| 뉴욕·런던·파리 | 보존 |
| 한복 디자이너 후보 | 보존 |

평탄화 형식도 의도대로 바깥(` | `)과 중첩(` ; `)이 구분된다.

```
[표6] 콘텐츠 제작 책임자: 박민재 | 중분류 / 담당 | 세부 내용(3단 표)
      ; 음원 제작 담당: 정다은 | 항목 | 조사 내용 | 기한
      ; 편곡 | 국악 편곡가 섭외 | 7/28
```

**`R202` 수정이 실제 문서로 실증됐다.** 수정 전이라면 `편곡`·`녹음`·`7/28`이 전부
사라졌을 내용이다.

### 추출: 상위 표만

```
상위 표 담당자 5명       → action 5건, owner 5/5   ✓
중첩 표 세부 과제 10여건  → 편곡·녹음·뮤직비디오·쇼케이스 장소·한복 디자이너·관객 동선  전부 ✗
```

중첩 표 행도 `항목 | 조사 내용 | 기한` 구조라 표5형에 가까운데 추출되지 않는다.
**한 블록에 3개 층위가 뒤섞이는 것**이 원인으로 보인다.

**텍스트 전달은 정상이다.** 실험으로 확인했다.

```
중첩 표 텍스트만 (300자)  → action 4/4, owner 4/4 정확히 추출
문서 전체 (3,393자)       → 같은 내용 0건
추출기 청크 수: 1 (분할되지 않음)
```

문제는 추출 단계의 인식이지 변환·전달이 아니다.
후속 이슈로 정리했다(`follow-up-issues.md` #1).

## 5. 깊이 상한 경계 (합성)

Word로 6단 중첩을 손으로 만드는 것은 비현실적이라 python-docx로 생성해 확인했다.

| 깊이 | 내용 | 경고 | 판정 |
| --- | --- | --- | --- |
| 4단 | 보존 | `table_flattened` | 상한 이내 |
| 5단 | 보존 | `table_flattened` | 상한 이내 |
| 6단 | 유실 | `unsupported_element` | 유실하되 **알림** |
| 7단 | 유실 | `unsupported_element` | 동일 |

경고 메시지: `중첩 깊이 5단을 초과한 표의 내용은 변환되지 않았습니다.`

**경계는 5/6 사이다.** 6단 이상이어도 **파일은 정상 업로드된다** — 그 표 내용만
버려지고 문서 전체는 `indexed`까지 간다.

에러가 되는 것은 **깊은 표가 문서의 유일한 내용일 때**뿐이다.

```
깊이5, 표만  → 변환 성공 (5단은 살아남아 블록이 생김)
깊이6, 표만  → 400 empty_document
              "문서에서 추출된 텍스트가 없습니다.
               중첩 깊이 5단을 초과한 표의 내용은 변환되지 않았습니다."
```

블록이 0개가 되면 `assemble()`이 오류를 던지되 **사유를 메시지에 싣는다.**
`R301`과 자체 점검 1번으로 확보된 동작이며, 이번 수정(`R401`·`R501`)이 깨지 않았다.

## 6. 자유 양식의 변동은 LLM 비결정성이다

정형은 안정적이고 자유 양식은 조합마다 결과가 다르다.

| | dec | iss | rsk |
| --- | ---: | ---: | ---: |
| 02 자유 원본 docx | 2 | 0 | 5 |
| 02 자유 원본 pdf | 2 | 1 | 5 |
| 04 자유 가명화 docx | 1 | 5 | 0 |
| 04 자유 가명화 pdf | 2 | 2 | 3 |

**초판 실행과 비교하면 원인이 드러난다.** 코드는 `extractor.py`·`ingestor.py`를
전혀 건드리지 않았는데도 분포가 바뀌었다.

| | 초판(`c507626`) | 현재(`d73bd04`) |
| --- | --- | --- |
| 01·03 정형 4조합 | 전부 `3/5/0/0` | `3/5/0/0` ×3, `4/5/0/0` ×1 |
| 02 자유 docx | `1/5/5/0` | `2/5/0/5` |
| 04 자유 docx | `2/5/1/5` | `1/5/5/0` |

**정형은 두 번 다 거의 동일하고 자유 양식만 크게 흔들린다.** 따라서 변동의 원인은
LLM 비결정성이며 포맷 차이나 코드 변경이 아니다.

초판 §7이 "LLM 비결정성인지 PDF 구조 소실인지 분리하지 못했다"고 남긴 질문이
이번 재실행으로 해소됐다.

### 왜 자유 양식만 흔들리나

**원문 자체가 모호하다.** "아직 정하지 못한 것"(미결)과 "기억해 둘 위험 신호"(위험)가
둘 다 있는데, 미결 항목은 issue로도 risk로도 분류될 수 있다. 실제로 실행마다
5건이 전부 issue가 되기도 하고 전부 risk가 되기도 했다.

문서를 쓰는 쪽에서 두 범주를 구분해 적지 않는 한 이 변동은 남는다.

## 7. 포맷 간 구조 차이 (참고)

같은 문서인데 DOCX와 PDF의 블록 구조가 다르다.

| | blocks | heading | list_item | table_row | paragraph |
| --- | ---: | ---: | ---: | ---: | ---: |
| 01 정형 docx | 63 | 9 | 9 | 28 | 17 |
| 01 정형 pdf | 26 | 0 | 0 | 0 | 26 |

PDF 변환기는 `pypdf`로 텍스트만 추출하므로 제목·목록·표 구분이 사라진다.
**설계상 그런 것이고 결함이 아니다.** 다만 출처 추적의 `heading` 경로가 PDF에서는
비게 된다(74청크 중 35건만 채움).

**정형 문서는 이 차이에도 action 추출이 동일**했다(docx·pdf 모두 5/5).

## 8. 완료 조건 대비

| WBS 완료 조건 | 판정 | 근거 |
| --- | --- | --- |
| DOCX 업로드·변환·메모리 생성 성공 | **충족** | 5건 `indexed`, memory 8~12행 |
| 텍스트 PDF 업로드·변환·메모리 생성 성공 | **충족** | 4건 `indexed`, memory 8~13행 |
| 출처 메타데이터 확인 가능 | **충족** | Chroma 74청크에 block/page/heading/format, `memory_sources` 90행 |
| 실패 파일은 명시적 오류 반환 | **충족** | 빈 파일 400 + `empty_document` |
| 전처리 정책서 작성 완료 | 충족 | `docs/DOCUMENT_INGESTION_POLICY.md` |

**5개 전부 충족.** 단 아래 유의사항이 붙는다.

- 완료 조건의 범위는 **"적재되는가"**이지 "추출 품질"이 아니다. §3·§7·§9의 한계는
  완료 조건 항목이 아니며 후속 이슈다.
- 익명화 실제 회의록 검증은 **합성 가명화본으로 대체**했다. E 담당자의 실제
  산출물이 아니므로 WBS의 해당 항목을 충족한다고 볼 수 없다.

## 9. 후속 조치

| # | 항목 | 성격 |
| --- | --- | --- |
| 1 | 표4형(속성이 열에 흩어진 표)·중첩 하위 행의 추출 | 기능 결함 |
| 2 | 업로드 경고 UI 표시 | 프론트엔드 |
| 3 | 동일 파일 반복 실행으로 추출 분산 측정 | 검증 방법 |
| 4 | 회의록 작성 가이드 — 위험·미결을 구분해 목록으로 | 운영 규칙 |
| 5 | 익명화 실제 회의록 확보 | E 담당자 |

상세는 `follow-up-issues.md` 참조.

**평탄화 형식 변경은 후속 조치에서 제외한다.** 실험으로 효과가 없음을 확인했다(§3).

## 10. 재현 방법

```bash
docker compose down -v && rm -rf .chroma       # 완전히 비우기
docker compose up -d
PAIM_AUTH_MODE=dev DEV_USER_ID=1 uv run uvicorn backend.main:app --host 127.0.0.1 --port 8000
# 파일 10건을 각각 새 프로젝트에 업로드 후 status 폴링, /memory 조회
```

`.env`에 `OPENAI_API_KEY`, `DB_PASSWORD` 필요. `SESSION_MEMORY_KEY`는 이 경로에
관여하지 않는다(lazy 초기화). `PAIM_AUTH_MODE=dev`는 로컬 검증 전용이다.
